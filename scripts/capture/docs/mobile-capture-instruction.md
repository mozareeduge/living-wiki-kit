Capture the following as exact user text. Do not summarize, title, tag,
interpret, or promote it. Return only the capture ID and status.

Route: Claude mobile dictation / Remote Control into the local session that
holds the governed wiki tools (`wiki_capture_text`, channel `claude-mobile`).
Result is `raw_media_available: false` — text only, no retained audio.

When to switch routes: if dictation looks wrong (especially Persian names,
numbers, dates), re-record through Telegram/Hermes or Obsidian so the raw
audio is preserved. Never present dictated text as a retained audio source.
