# Rough capture — <% tp.date.now("YYYY-MM-DD HH:mm") %>

Write the note raw. Do not tidy, summarize, or title it.

Run `capture-text` with channel `obsidian` afterwards; keep the capture ID here:

- capture:
- language: fa / en / mixed
