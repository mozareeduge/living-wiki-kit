# Voice capture — <% tp.date.now("YYYY-MM-DD HH:mm") %>

Record with Audio Recorder (embeds below), then run `capture-media`
(kind `voice`, channel `obsidian`). The audio file is the evidence;
any transcript below it is a draft until reviewed.

Embedded recording placeholder: `![[recording]]` (Obsidian inserts the audio
file here when the template is applied).

- capture:
- transcript status: pending / complete / needs-review
