# Handwriting / drawing — <% tp.date.now("YYYY-MM-DD HH:mm") %>

Attach the photo/scan (embeds below), then run `capture-media`
(kind `handwriting` / `drawing`, channel `obsidian`).
The image is the evidence; extraction and description stay separate.

Embedded image placeholder: `![[photo]]` (Obsidian inserts the attachment here
when the template is applied).

- capture:
- visible labels (literal, no guessing):
- description (interpretation, not a substitute):
